/*
    Emptied for the ETM build: Audio Unit (AU) is disabled (JucePlugin_Build_AU = 0)
    and the original Rez resources pulled in CoreAudio/Carbon headers (AUResources.r)
    that no longer exist in modern Xcode, breaking the build. We target VST3 on macOS,
    so these AU resources are not needed.
*/
